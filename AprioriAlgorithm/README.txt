You can run the algorithm with named 'transaction.csv' file that is placed in same folder, on terminal by typing python apriori.py

Minimum support identified 0.3 by default.

You can change it by adding the minimum support rate on last line of 'transaction.csv' file